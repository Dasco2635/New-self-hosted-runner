name: Test Runner

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on:
      - self-hosted
      - Windows
      - X64

    steps:
      - uses: actions/checkout@v3

      - run: echo "Test du Runner sous MS Windows 11"
``